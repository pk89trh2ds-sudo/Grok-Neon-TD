node_modules/
.project_id
.github_repo
.env
.env.*
# Portal build output (npm run build:portal) — generated on demand, zipped
# for upload to Poki/CrazyGames/itch.io. Not deployed anywhere from git.
.output/
neon-td-portal-build.zip

# Vercel build output (npm run build / vite build) — regenerated on every
# build and deploy. The initial scaffold committed it; a checked-in build
# goes stale against src/ immediately, so it is untracked now.
.vercel/output/
